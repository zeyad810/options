{% extends "layout.twig" %}


{% block header %} {{ include('header.twig') }} {% endblock %}

{% block main_content %}
    {% if (page.slug is defined and page.slug == 'options') or (page.handle is defined and page.handle == 'options') %}
      Lorem ipsum, dolor sit amet consectetur adipisicing elit. Eaque perferendis dicta sunt. Dicta ad natus eum tempore deleniti dolor eius mollitia, a neque quod. Deserunt totam magnam iure quaerat cumque.
    {% else %}
        <section class="blog mt-5 mb-5 ">
            <div class="container">

                <div class="blog-title mt-5 mb-5">
                    <h1>
                        {{ page.title }}
                    </h1>
                </div>
                <div class="blog-text mt-3 mb-5">
                    {{ page.content | raw }}
                </div>


            </div>
        </section>
    {% endif %}

{% endblock %}

{% block footer %} {{ include('footer.twig') }} {% endblock %}
