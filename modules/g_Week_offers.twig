<section class=" offer-deadline" section-id="{{ sectionId }}">

  <!-- Wrapper for all products -->
  <swiper-container  pagination="true"  class="productgroup">

    {% for product in settings.products.products %}
   
    <!-- Each product slide -->
    <swiper-slide class=" product-end">

      <!-- Product data section (left half) -->
      <div class="productdata">
       <div class="pro_cont">
         <h2>عرض الأسبوع</h2>
        <a class="title" href="/categories/{{ product.categories[0].id }}/{{ product.categories[0].slug }}"></a>       
        <p>{{ product.name }}</p>
        
        
        <!-- Countdown timer section -->
        <div class="counttimer">
          <div class="countDown">
            <ul>
              <li class="countdown__seconds js-seconds" style="color: white;">
                <span style="color: white; font-size: 25px; font-weight: 700"></span>
                {{ locals.sec }}
              </li>       
              <li class="countdown__minutes js-minutes" style="color: white;">
                <span style="color: white; font-size: 25px; font-weight: 700"></span>
                {{ locals.min }}
              </li>
              <li class="countdown__hours js-hours" style="color: white;">
                <span style="color: white; font-size: 25px; font-weight: 700"></span>
                {{ locals.hour }}
              </li>
              <li class="countdown__days js-days" style="color: white;">
                <span style="color: white; font-size: 25px; font-weight: 700"></span>
                {{ locals.day }}
              </li>
            </ul>
          </div>
        </div>
                <a href='{{product.html_url}}'>
                        <button class="shopbtn">تسوقي الآن</button>
                </a>
       </div>
      </div>
     

      <!-- Product image section (right half) -->
      <div class="productimage">
        <img loading="lazy" src="{{ product.images[0]['image']['medium'] }}" alt="{{ product.images.alt_text }}">
      </div>

    </swiper-slide> <!-- End of product slide -->

    {% endfor %}
    
  </swiper-container> <!-- End of swiper-wrapper -->

  <!-- Swiper navigation -->
  <div class="swiper-button-prev"></div>
  <div class="swiper-button-next"></div>

</section>

<style>
.offer-deadline {
    width: 100%; 
}

.productgroup {
    display: flex;
}

.product-end {
    display: flex; 
    width: 100%;
    height: 700px; 
}

.productdata {
    width: 50%; 
    height: 100%;
    background-color: #CB657A;
    color: white;
    display: flex;
    flex-direction: column;
    justify-content: center; 
    align-items: flex-start; 
    padding: 0 20px; 
    gap: 10px;
}
.productdata .pro_cont{
    position: relative;
    right: 25%;
}
.productdata h2 {
    font-size: 42px;
    font-weight:bold;
    margin-bottom: 10px;
    padding-right: 40px; 
    position: absolute;

}
.productdata a{
      padding-right: 40px; 
      margin-top:10px;
}

.productdata p{
       font-size: 22px;
       font-weight:medium;
       margin-top:20px;
       margin-bottom:20px;
      padding-right: 40px; 
      display:flex;
      justify-content:start
}
.productdata .counttimer {
      padding-right: 25px;
}
.countDown ul {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    margin-top:20px;
}
.countDown li {
    display: flex;
    flex-direction: column; 
    align-items: center; 
    justify-content: center; 
    margin: 0 15px; 
    padding: 40px; 
    border: 2px solid #FFCBD9;
    border-radius: 10px; 
    text-align: center; 
    height: 50px; 
    width: 50px; 
}
.shopbtn{
    position: absolute;
    right: 40px;
    bottom: -60px;
}
.productimage {
    width: 50%; 
    display: flex;
    justify-content: center;
    align-items: center; 
}

.productimage img {
    width: 100%; 
    height: 100%; 
    object-fit: cover; 
}

.productdata button {
  background-color:white;
  width:173px;
  height:65px;
  text:16px;
  color:#CE5A67;
  font-weight:bold;
  border-radius:10px;
}

/*swiper pagenation style*/
.swiper-pagination {
    position: absolute; 
    bottom: 10px; 
    left: 50%; 
    transform: translateX(-50%); 
    display: flex;
    gap: 5px; 
}

.swiper-pagination-bullet {
    width: 30px; 
    height: 10px;
    background-color: #FFCBD9; 
    border-radius: 5px; 
    opacity: 1; 
}

.swiper-pagination-bullet-active {
    background-color: #CB657A; 
}
@media (max-width: 450px) {
    .product-end {
        flex-direction: column-reverse !important; /* Stack vertically */
        height: auto; /* Adjust height */
    }

    .productdata {
        width: 100%;
        height: 50%;
        text-align: center;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
    }

    .productdata .pro_cont {
        position: relative;
        right: 0;
        text-align: center;
    }

    .productdata h2 {
        font-size: 24px;
        position: static;
        padding-right: 0;
        margin-bottom: 10px;
        position: absolute;
    }

    .productdata p {
        font-size: 14px;
        margin-top: 10px;
        margin-bottom: 10px;
        position: absolute;
        top: 30px;
        right: -40px;
    }

    .productdata button {
        width: 140px;
        height: 50px;
        font-size: 14px;
        position: static;
        margin-top: 30px;
        text-align: center;
    }
    .productdata a {
    padding-right: 0px; }
    .countDown {
        font-size: 14px; /* Adjust font size of the countdown */
    }

    .countDown ul {
        margin-top: 10px; /* Adjust margin */
    }

    .countDown li {
        padding: 30px;
        font-size: 14px;
        height: 60px;
        width: 60px;
    }

    .productimage {
        width: 100%;
        height: 50%;
        display: flex;
        justify-content: center;
    }
    .productdata .counttimer{
      padding-right: 0px;
      margin-top: 23%;
    }
    .productimage img {
        width: 100%; /* Full width */
        height: auto; /* Adjust height */
    }
    .counttimer .countDown ul li span {
      font-size: 14px !important;
      font-weight: 500 !important;
    }
}
</style>
  <script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-element-bundle.min.js"></script>
<script>

document.addEventListener('DOMContentLoaded', function() {
  const swiperEl = document.querySelector(' swiper-container');
  
  if (swiperEl && swiperEl.shadowRoot) {
    const activeBullet = swiperEl.shadowRoot.querySelector('.swiper-pagination-bullet-active');
    
    if (activeBullet) {
      activeBullet.style.backgroundColor = '#CB657A'; 
    }
  }
});

function convertToDate(textDate) {
 
  if (!textDate || !textDate.trim()) {
    return null; 
  }

  const parts = textDate.split('-');
  if (parts.length !== 3) {
    return null; 
  }

  const year = parseInt(parts[0], 10);
  const month = parseInt(parts[1], 10) - 1; 
  const day = parseInt(parts[2], 10);

  return new Date(year, month, day);
}
let realtime = convertToDate('{{ settings.dateTime_enter }}');
let count = new Date(realtime).getTime();
let total = document.querySelectorAll('.countDown');
total.forEach( (el) =>{
    
    if (el) {
    const dayscount = el.querySelector('.js-days span');
    const hourscount = el.querySelector('.js-hours span');
    const minutescount = el.querySelector('.js-minutes span');
    const secondcount = el.querySelector('.js-seconds span');

    const startCountdown = () => {
    const timer = setInterval(function() {

    let now = new Date().getTime();

    let distance = count - now;
    if (distance < 0) {
        el.remove();
        document.querySelectorAll('.counttimer').forEach(e => {
            e.innerHTML = `
            <h1 class="offer_end">
                انتهي العرض
            </h1>
            `
        })
        clearInterval(timer);
    }

    let days = Math.floor(distance / (1000 * 60 * 60 * 24));
    let hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
    let minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
    let seconds = Math.floor((distance % (1000 * 60)) / 1000);


    dayscount.innerHTML = days;
    hourscount.innerHTML = hours;
    minutescount.innerHTML = minutes;
    secondcount.innerHTML = seconds;



    }, 1000);
    }
    startCountdown();
    }
})
</script>

{% schema %}

{
    "name": {
        "ar": "  عرض الأسبوع",
        "en": "Week Offers "
    },
    "icon": "fal fa-user",
    "display": true,
    "settings": {
        "title" :{
            "type":"text",
            "label":{
                "ar":" العنوان",
                "en":"title"
            }
        }, 
        "all" :{
            "type":"url",
            "label":{
                "ar":" عرض الكل",
                "en":"show all"
            }
        },
        "products": {
            "type": "products",
            "label": {
                "ar": "اختر قائمة المنتجات ",
                "en": "Select category products"
            }   
        },
        "dateTime": {
                "type": "fieldset",
                "label": {
                    "ar": "ادخل تاريخ الانتهاء",
                    "en": "date for emd offer"
                },
                "expandable": true,
                "settings": {
                    "enter":{
                    "type":"text",
                    "info":{
                        "ar":"YYYY-MM-DD",
                        "en":"YYYY-MM-DD"
                    }
                    },
                    "shop_button":{
                      "type":"text",
                      "label":{
                      "er":"نص الزر",
                      "en" :"button text"

                      }


                    }
                }
            }
    }
}

{% endschema %}


